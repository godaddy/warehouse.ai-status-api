#!/usr/bin/env node

require('..').start(function (err, app) {
  if (err) {
    return app.log.error('Error starting warehouse-status-api', err);
  }
  app.log.info('Warehouse-status-api started on %s', app.config.get('http'));
});
